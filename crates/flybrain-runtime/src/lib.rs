//! MAS runtime skeleton.
//!
//! Phase 0 ships only the placeholder; full message bus, scheduler, and
//! trace writer arrive in Phase 2.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder_compiles() {
        assert_eq!(2 + 2, 4);
    }
}
